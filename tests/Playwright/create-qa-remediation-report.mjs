import fs from 'node:fs/promises';
import path from 'node:path';
import { chromium } from 'playwright';

const root = path.resolve(new URL('.', import.meta.url).pathname, '../..');
const source = path.join(root, 'public/assets/.doc/qa-retest-2026-09-05/qa-retest-results.json');
const data = JSON.parse(await fs.readFile(source, 'utf8'));
const out = path.join(root, 'public/assets/.doc/qa-retest-2026-09-05');
const esc = v => String(v ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const table = (heads, rows, widths) => `<table><colgroup>${widths.map(w=>`<col style="width:${w}%">`).join('')}</colgroup><thead><tr>${heads.map(esc).map(x=>`<th>${x}</th>`).join('')}</tr></thead><tbody>${rows.map(r=>`<tr>${r.map(x=>`<td>${esc(x)}</td>`).join('')}</tr>`).join('')}</tbody></table>`;
const all = data.reports.flatMap(r => r.checks);
const failed = all.filter(c => c.status === 'FAIL');
const blocked = all.filter(c => c.status === 'BLOCKED');
const screenshotFor = async check => {
 const evidence = String(check.evidence || '');
 if (!/\.png$/i.test(evidence)) return null;
 const file = path.join(root, 'tests/Playwright', evidence);
 try {
  const image = await fs.readFile(file);
  return {file: evidence, src: `data:image/png;base64,${image.toString('base64')}`};
 } catch { return null; }
};
const summary = [
 ['R1','High','Technician work-orders page fails with HTTP 500.','PORTALS-0081/82; AUTH-022/24','Trace the server exception and dependency/query failure; add a friendly 5xx boundary and controller test.'],
 ['R2','High','Login is available over HTTP without HTTPS redirect.','AUTH-025','Force HTTPS at reverse proxy and application layer; enable HSTS only after HTTPS is reliable.'],
 ['R3','High','Plan CRUD responses are inconsistent with persistence. Create returned 500 while ID 79 existed; update/delete returned 422.','ADMIN-094/96/97/98','Make service transactions and response status match commit outcome; validate exact payload contract; add create-update-delete integration coverage.'],
 ['R4','Medium','Invalid/missing CSRF is rejected but returns HTTP 500 instead of 419/403.','AUTH-034/35/47/48/60/61','Handle CSRF exceptions at the API boundary with a consistent 419 or 403 JSON response and no error-level server failure.'],
 ['R5','Medium','Technician pages request notifications and receive HTTP 403.','PORTALS-0076/88','Separate portal notification feed permissions from admin/security events; return an allowed maintenance-only feed or avoid the request.'],
 ['R6','Medium','Collapsed/mobile sidebar notification bell is outside the viewport and click times out.','ADMIN-107; PORTALS-0066/97','Unify mobile drawer and compact-rail state; constrain topbar layout at 390px; verify bell bounds after toggle.'],
 ['R7','Low','Six admin pages contain unnamed combobox/switch controls.','ADMIN-007/43/46/52/55/70','Add explicit labels or aria-label/aria-labelledby tied to visible field labels; add accessibility assertions.'],
];
const html = `<!doctype html><html><head><meta charset="utf-8"><title>NexusBox QA remediation plan</title><style>
@page{size:A4 landscape;margin:15mm 14mm 16mm}*{box-sizing:border-box}body{font-family:Arial,sans-serif;color:#172033;font-size:10pt;line-height:1.38;margin:0}h1{font-size:25pt;color:#17426d;margin:0 0 12px}h2{font-size:16pt;color:#17426d;margin:22px 0 9px}h3{font-size:12pt;color:#17426d;margin:15px 0 6px}p{margin:7px 0}.meta{background:#f1f5f9;border-left:4px solid #2563eb;padding:12px;margin-bottom:15px}table{width:100%;border-collapse:collapse;table-layout:fixed;margin:8px 0 16px}thead{display:table-header-group}tr{break-inside:avoid}th,td{border:1px solid #cbd5e1;padding:7px;vertical-align:top;overflow-wrap:anywhere;word-break:normal}th{background:#e7eef7;text-align:left;font-size:9.5pt}td{font-size:8.7pt}.screenshots{display:grid;grid-template-columns:1fr 1fr;gap:12px}.screenshots figure{break-inside:avoid;border:1px solid #cbd5e1;padding:8px;margin:0}.screenshots figure h3{font-size:10pt;margin:0 0 6px}.screenshots img{display:block;width:100%;max-height:105mm;object-fit:contain;object-position:top;background:#f8fafc}.screenshots figcaption{font-size:8pt;color:#526175;margin-top:4px;overflow-wrap:anywhere}.pill{font-weight:bold}.small{font-size:8.5pt;color:#526175}li{margin:5px 0}.pass{color:#166534}.fail{color:#b91c1c}.blocked{color:#92400e}
</style></head><body><h1>NexusBox — QA findings and remediation plan</h1><div class="meta">Source: ${esc(source)}<br>Target: http://10.0.10.155/login<br>Scope: Admin, Subscriber, Technician, authentication/API security<br>Original execution: Playwright Chromium, 5 September 2026<br>Recorded result: <b>237 passed · 28 failed · 11 blocked/inconclusive · 276 checks</b></div><h2>Executive conclusion</h2><p>The system is not ready for production sign-off. The most important defects are the technician work-orders HTTP 500, HTTP transport without HTTPS enforcement, inconsistent Plan CRUD responses, and server-error handling for rejected CSRF requests. The notification and compact-sidebar failures are separate UI/authorization issues. Several repeated checks represent one shared defect, so the seven root causes below are more useful than counting each symptom as a separate bug.</p>${table(['ID','Priority','Consolidated root problem','Evidence','Proposed solution'],summary,[7,9,25,20,39])}<h2>What passed</h2><p>All three valid login flows reached their expected portals; session rotation, logout replay rejection, role boundaries, protected anonymous access, harmless SQL-injection input rejection, security headers, mobile overflow on most working pages, subscriber detail modals, and Scheduled Downtime disposable CRUD passed within their stated scope.</p></body></html>`;
await fs.writeFile(path.join(out,'qa-remediation-plan.html'),html);
await fs.writeFile(path.join(out,'qa-remediation-findings.json'),JSON.stringify({generatedAt:new Date().toISOString(),source,counts:data.totals,rootCauses:summary,failedCheckIds:failed.map(c=>c.id),blockedCheckIds:blocked.map(c=>c.id)},null,2));
const figures = [];
for (const check of failed) { const shot = await screenshotFor(check); figures.push(`<figure><h3>${esc(check.id)} — ${esc(check.name)}</h3>${shot ? `<img src="${shot.src}" alt="Playwright screenshot for ${esc(check.id)}"><figcaption>${esc(shot.file)}</figcaption>` : '<p class="small">No screenshot artifact was recorded for this check. See the machine-readable JSON evidence.</p>'}</figure>`); }
let full = html.replace('</body>', `<section><h2>Failed test screenshots</h2><p>These are the screenshots captured by the retest. Repeated screenshots are retained because each check is a separate recorded assertion. Checks without an image artifact are explicitly marked.</p><div class="screenshots">${figures.join('')}</div></section><section><h2>Implementation plan</h2><h3>Phase 0 — reproduce and instrument</h3><ol><li>Capture the exact exception and SQL/query inputs for the work-orders controller in a protected server log; do not expose internals to clients.</li><li>Re-run Plan CRUD with a unique disposable fixture and record database state plus HTTP response at each step.</li><li>Confirm deployed proxy scheme, trusted-proxy configuration, and session cookie Secure behavior.</li></ol><h3>Phase 1 — stabilize server contracts</h3><ol><li>Fix work-orders dependency/query/controller failure; return 200 for a valid page or a controlled error page with no uncaught 500.</li><li>Correct Plan create/update/delete validation and transaction boundaries so response status reflects persistence; guarantee cleanup.</li><li>Add centralized CSRF exception mapping to 419/403 JSON for API requests.</li><li>Define notification authorization: subscriber and technician feeds contain only permitted maintenance notices; login-attack notices stay out of subscriber feeds.</li></ol><h3>Phase 2 — fix shell and accessibility</h3><ol><li>Model sidebar states explicitly: expanded, compact rail, and mobile drawer. Section expansion must only affect labels/items in expanded mode.</li><li>At 390px, keep the bell inside the topbar viewport or move it into the drawer; test open, close, keyboard focus, and modal stacking.</li><li>Label every combobox and switch and assert accessible names in Playwright.</li></ol><h3>Phase 3 — deploy hardening and regression</h3><ol><li>Redirect HTTP to HTTPS at the edge and application fallback; then verify HSTS and Secure cookies.</li><li>Run the full suite sequentially per account to avoid session invalidation and include a controlled 30-minute expiry test in a staging environment.</li><li>Expand disposable CRUD fixtures for tickets/work-orders before testing their mutation paths; never guess existing IDs.</li></ol><h2>Acceptance criteria for closure</h2><ul><li>Work-orders and all permitted portal pages load without 4xx/5xx runtime errors.</li><li>Plan and downtime create, read, update, reload, delete, and old-record-preservation checks pass with truthful HTTP responses.</li><li>Missing/invalid CSRF returns 419/403, never 500; no internal exception text is exposed.</li><li>Only authorized maintenance notifications appear in subscriber/technician feeds; unread badge behavior remains persistent.</li><li>Every menu icon is visible in compact mode at desktop and mobile widths; bell and modal are reachable and above the sidebar.</li><li>HTTPS redirect, Secure/HttpOnly/SameSite cookies, role boundaries, login abuse resistance, and injection rejection pass in a clean staging run.</li></ul><h2>Test limitations</h2><p>CRUD was fully exercised only for disabled Plans and Scheduled Downtime fixtures; ticket/work-order mutation was blocked because no safe cleanup endpoint or fixture existed. No charges, network deployment, live global settings, or existing-record deletion was attempted. The SQL-injection checks were harmless rejection probes, not an exhaustive penetration test. The report is a remediation plan, not a claim that the defects are fixed.</p></section></body>`);
const browser=await chromium.launch({headless:true});try{const page=await browser.newPage();await page.setContent(full);await page.pdf({path:path.join(out,'qa-remediation-plan.pdf'),format:'A4',landscape:true,printBackground:true,displayHeaderFooter:true,headerTemplate:'<span></span>',footerTemplate:'<div style="width:100%;font:9px Arial;color:#64748b;text-align:center">NexusBox QA remediation plan · <span class="pageNumber"></span> / <span class="totalPages"></span></div>'});console.log(JSON.stringify({pdf:path.join(out,'qa-remediation-plan.pdf'),checks:all.length,rootCauses:summary.length}));}finally{await browser.close()}
