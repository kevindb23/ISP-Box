<?php

namespace App\Modules\Auth;

class Module
{

    /*
    |--------------------------------------------------------------------------
    | Register Module Services
    |--------------------------------------------------------------------------
    |
    | Your container already supports automatic dependency resolution.
    | That means Controllers → Services → Repositories → DatabaseConnection
    | will be resolved automatically via Reflection.
    |
    | So we don't need to manually bind anything here.
    |
    */

    public function register($container)
    {
        // No bindings required
    }

}
