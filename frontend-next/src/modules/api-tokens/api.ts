import type{ApiEnvelope}from'../../lib/api';export async function get<T>(url:string):Promise<ApiEnvelope<T>>{const response=await fetch(url,{credentials:'same-origin',headers:{Accept:'application/json'}}),envelope=await response.json() as ApiEnvelope<T>;if(!response.ok||envelope.success===false)throw new Error(envelope.message||'Request failed ('+response.status+')');return envelope;}export async function post<T>(url:string,payload:Record<string,unknown>={}):Promise<ApiEnvelope<T>>{const token=document.querySelector<HTMLMetaElement>('meta[name="csrf-token"]')?.content||'',response=await fetch(url,{method:'POST',credentials:'same-origin',headers:{Accept:'application/json','Content-Type':'application/json',...(token?{'X-CSRF-TOKEN':token}:{})},body:JSON.stringify(payload)}),envelope=await response.json() as ApiEnvelope<T>;if(!response.ok||envelope.success===false)throw new Error(envelope.message||'Request failed ('+response.status+')');return envelope;}
